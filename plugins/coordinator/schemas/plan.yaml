schema: plan
applies_to: "docs/plans/*.md"
required:
  title: string
  created: iso-date
  author: string
  status:
    type: enum
    values: [draft, reviewed, approved, executing, deferred, shipped, abandoned, superseded]
optional:
  branch: string
  reviewer: string
  source: string
  superseded_by: string
