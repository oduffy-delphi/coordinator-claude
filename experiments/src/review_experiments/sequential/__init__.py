"""Sequential vs parallel review experiment — does our architecture produce better results?

Experiment-specific modules. Uses shared infrastructure from the parent package.
"""

from .config import Arm, EXPERIMENT_ID
